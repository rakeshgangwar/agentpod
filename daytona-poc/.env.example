# Daytona POC Environment Configuration

# Daytona API URL (self-hosted)
DAYTONA_API_URL=http://localhost:3000

# Daytona API Key (get from dashboard after login)
# Leave empty for initial setup - we'll use cookie auth via browser login
DAYTONA_API_KEY=

# OpenCode sandbox image (pushed to local registry)
OPENCODE_IMAGE=localhost:6000/opencode-sandbox:latest

# OpenCode port inside sandbox
OPENCODE_PORT=4096
